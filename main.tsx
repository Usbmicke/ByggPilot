import React from 'react';
import ReactDOM from 'react-dom/client';
import ByggPilotExact from './ByggPilotExact.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ByggPilotExact />
  </React.StrictMode>
); 